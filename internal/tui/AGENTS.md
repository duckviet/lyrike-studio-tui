# Terminal UI

- Keep Bubble Tea models focused on state transitions and view rendering.
- Keep domain mutations in internal domain packages, not in view code.
- Drive user-facing TUI behavior with tmux transcripts for manual QA evidence.
- Avoid layout changes that let panel text overlap at small terminal sizes.

